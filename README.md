# ctdaemon - Crypto Trading System Daemon v2

**Высокопроизводительный демон для автоматического мониторинга и торговли криптовалютами на децентрализованных и централизованных биржах.**

---

## 📋 Описание

`ctdaemon` — это Go приложение которое работает как служба (daemon) для:
- **Мониторинга** стакана ордеров (orderbook) с разных крипто-бирж
- **Торговли** по автоматизированным стратегиям (grid, DCA, scalping)
- **Управления позициями** с контролем рисков
- **Хранения исторических данных** в ClickHouse
- **REST API управления** демоном через HTTP

Архитектура поддерживает работу в трёх режимах:
- **monitor** — только сбор данных с бирж в ClickHouse
- **trader** — только исполнение торговых стратегий
- **both** — одновременный мониторинг и торговля

---

## 🚀 Быстрый старт

### Требования
- **Go** 1.25.4 или выше
- **MySQL** 8.0+ или **PostgreSQL** 13+
- **ClickHouse** 23+ (для хранения исторических данных)
- Доступ к крипто-биржам API ключи (Binance, Bybit, OKX, Kucoin и т.д.)

### Установка
```bash
# Клонирование репозитория
git clone https://github.com/titaev-lv/daemon2.git
cd daemon2

# Установка зависимостей
go mod download

# Компиляция
go build -o daemon cmd/daemon/main.go

# Запуск
./daemon
```

### Конфигурация
Скопируйте и отредактируйте файл конфигурации:
```bash
cp conf/config.example.ini conf/config.ini
# Отредактируйте conf/config.ini с вашими параметрами
```

Основные параметры в `conf/config.ini`:
```ini
[database]
type = mysql              # mysql или postgres
host = localhost
port = 3306
user = root
password = password
name = ct_system
use_tls = yes             # Рекомендуется для production
max_retries = 5           # Критичный параметр!

[server]
port = 8080               # REST API порт

[log]
level = debug             # debug, info, warn, error
dir = ./logs

[role]
mode = both               # monitor, trader или both

[monitor]
orderbook_depth = 20      # Глубина стакана: 20, 50 или 0 (full)
batch_size = 500
batch_interval_sec = 5
ring_buffer_size = 10000

[trader]
max_open_orders = 10
max_position_size = 1000.0  # USDT
default_strategy = grid      # grid, dca, scalp

[clickhouse]
host = localhost
port = 8123
database = crypto
username = default
password = default
```

---

## 📁 Структура проекта

```
daemon2/
├── cmd/
│   └── daemon/
│       ├── main.go              # Точка входа, инициализация
│       └── ctdaemon             # Скомпилированный бинарник
├── conf/
│   ├── config.example.ini       # Пример конфигурации (полный)
│   ├── config.ini               # Рабочая конфигурация (НЕ коммитить!)
│   └── ssl/                     # TLS сертификаты (опционально)
├── internal/
│   ├── api/
│   │   └── server.go            # REST API endpoints
│   ├── config/
│   │   └── config.go            # Загрузка конфигурации
│   ├── db/
│   │   ├── factory.go           # Создание подключения к БД
│   │   ├── mysql.go             # MySQL драйвер с TLS
│   │   └── postgres.go          # PostgreSQL драйвер с TLS
│   ├── logger/
│   │   └── logger.go            # Структурированное логирование
│   ├── manager/
│   │   └── manager.go           # Управление жизненным циклом
│   ├── state/
│   │   └── state.go             # Сохранение состояния демона
│   └── core/
│       ├── exchange/
│       │   ├── types.go          # Типы данных (Order, Trade и т.д.)
│       │   └── drivers/          # Драйверы для 8 бирж
│       └── messaging/
│           └── message.go        # Унифицированный формат сообщений
├── logs/                        # Логи приложения
├── state/
│   └── daemon.state             # Состояние демона (JSON)
├── go.mod                       # Go модули
├── go.sum                       # Хешибьем зависимостей
├── README.md                    # Этот файл
├── CODE_STRUCTURE.md            # Подробное описание структуры
├── PHASE_1_2_TYPES_REFERENCE.md # Справочник типов Phase 1.2
└── PHASE_1_3_DETAILED.md        # Описание Phase 1.3 (конфигурация)
```

---

## 🌐 REST API

Демон предоставляет HTTP REST API для управления:

### Контроль демона
```bash
# Запустить демон
curl -X PUT http://localhost:8080/control?action=start

# Остановить демон (graceful shutdown, 30 сек)
curl -X PUT http://localhost:8080/control?action=stop
```

### Статус
```bash
# Получить текущий статус
curl http://localhost:8080/status
# Ответ:
# {
#   "running": true,
#   "uptime": "2h 30m",
#   "start_time": "2023-12-11T10:00:00Z",
#   "shutdown_time": null,
#   "error": null
# }
```

### Информация
```bash
# Получить версию
curl http://localhost:8080/version
# Ответ:
# {
#   "version": "2.0.1",
#   "go_version": "1.25.4",
#   "timestamp": "2023-12-11T12:00:00Z"
# }
```

---

## 🏗️ Архитектура

### Инициализация
При запуске демон выполняет следующие этапы (в порядке):
1. **Load Config** — загрузка конфигурации из `config.ini`
2. **Init Logger** — инициализация логирования со своим форматом
3. **Connect DB** — подключение к MySQL/PostgreSQL с retry логикой
4. **Init Manager** — создание менеджера жизненного цикла
5. **Start API** — запуск HTTP сервера на указанном порту
6. **Setup Signals** — обработка SIGINT/SIGTERM для graceful shutdown
7. **Load State** — загрузка предыдущего состояния (если есть)
8. **Start Workers** — запуск горутин для монитора/трейдера

### Graceful Shutdown
При получении сигнала на завершение (Ctrl+C, SIGTERM):
- Останавливает приём новых запросов (API)
- Закрывает открытые позиции/ордера
- Сохраняет состояние в `state/daemon.state`
- Закрывает БД соединения
- **Timeout:** максимум 30 секунд на завершение

### Компоненты

#### Config (`internal/config/config.go`)
- Парсит конфигурацию из INI файла
- Поддерживает режимы: monitor, trader, both
- Конфигурация для MySQL, PostgreSQL, ClickHouse
- Параметры мониторинга и торговли с defaults

#### Logger (`internal/logger/logger.go`)
- Структурированное логирование через `slog`
- Текстовый формат с цветом (не JSON)
- Ротация логов по размеру
- Отдельные файлы: `error.log`, `trade.log`

#### Database (`internal/db/`)
- **factory.go** — создание подключения с exponential backoff retry
- **mysql.go** — MySQL драйвер с TLS/SSL поддержкой
- **postgres.go** — PostgreSQL драйвер с TLS/SSL поддержкой
- Пул соединений: MaxOpenConns=20, MaxIdleConns=5

#### Manager (`internal/manager/manager.go`)
- Управление жизненным циклом демона
- Start/Stop методы
- Context-based synchronization
- Graceful shutdown с таймаутом

#### State (`internal/state/state.go`)
- Сохранение состояния в JSON файл (`state/daemon.state`)
- Singleton pattern с sync.Once
- Auto-resume при перезапуске

#### API Server (`internal/api/server.go`)
- REST endpoints для управления демоном
- PUT /control?action=start/stop
- GET /status (uptime, running state)
- GET /version (версия приложения)

---

## 🔐 Безопасность

### TLS/SSL для БД
- **MySQL:** Поддерживает mTLS (взаимная аутентификация)
- **PostgreSQL:** Поддерживает режимы SSL: disable, require, verify-ca, verify-full
- **Рекомендуется:** `use_tls = yes` + `verify-ca` для production

### API Security
- Демон слушает только локально (127.0.0.1:8080)
- Нет аутентификации (предполагается локальная сеть)
- Для публичного API нужен reverse proxy (nginx с auth)

### Конфиденциальность
- Пароли БД в `config.ini` (НЕ коммитить!)
- API ключи бирж в переменные окружения или secure vault
- Логи содержат sensitive data (обрезать для production)

---

## ⚡ Производительность

### Оптимизация
- **Ring Buffer:** кэширование последних данных в памяти
- **Batch Processing:** отправка обновлений батчами в ClickHouse
- **Connection Pooling:** переиспользование БД соединений
- **Async Processing:** горутины для параллельной работы

### Масштабируемость
- **Timestamp:** микросекунды (1 μs = 10^-6 sec) для точности HFT
- **ClickHouse:** оптимизирована для аналитики больших объемов данных
- **Multiple Exchanges:** независимые горутины для каждой биржи

---

## 📊 Типы данных

### Унифицированный формат (Phase 1.2)
Все данные от разных бирж преобразуются в унифицированный формат:

```go
// Orderbook (стакан ордеров)
type Level struct {
    Price     float64
    Amount    float64
    Timestamp int64    // microseconds
}

// Message (унифицированное сообщение)
type Message struct {
    Type      string    // "orderbook", "trade", "order", "position"
    ExchangeID string   // "binance", "bybit", "okx"
    Pair      string    // "BTC/USDT"
    Timestamp int64     // microseconds (UTC)
    Data      []byte    // JSON
}
```

---

## 🔌 Поддерживаемые биржи (Phase 1.5+)

Планируется поддержка:
- ✅ **Binance** (spot + futures)
- ✅ **Bybit** (spot + futures)
- ✅ **OKX** (spot + futures)
- ✅ **Kucoin** (spot + futures)
- ✅ **Coinex** (spot)
- ✅ **HTX** (spot + futures, бывший Huobi)
- ✅ **MEXC** (spot + futures)
- ⏳ **DEX** (Uniswap V3, 1inch и т.д.)

---

## 📈 Развитие (Roadmap)

### Phase 1 (текущая) - Фундамент
- ✅ Phase 1.1: Структура проекта
- ✅ Phase 1.2: Типы данных (Order, Trade и т.д.)
- ✅ Phase 1.3: Конфигурация с поддержкой режимов
- ⏳ Phase 1.4: MySQL схема (MONITORING, TRADE, TRADE_HISTORY)
- ⏳ Phase 1.5: Exchange driver interface + Binance реализация
- ⏳ Phase 1.6-1.8: Остальные биржи (Bybit, OKX, Kucoin и т.д.)

### Phase 2 - Стратегии
- Grid Trading
- Dollar Cost Averaging (DCA)
- Scalping с микро-ордерами

### Phase 3 - Анализ
- Риск-менеджмент
- Backtesting
- Performance метрики

### Phase 4+ - Расширение
- WebSocket live updates
- Mobile app для управления
- Machine learning стратегии

---

## 🛠️ Разработка

### Требования для разработки
```bash
go version              # Go 1.25.4+
golangci-lint --version # Линтер
go test ./...           # Тесты
```

### Построение
```bash
# Debug версия
go build -o daemon cmd/daemon/main.go

# Release версия
go build -ldflags="-s -w" -o daemon cmd/daemon/main.go
```

### Тестирование
```bash
# Запуск всех тестов
go test ./...

# С покрытием
go test -cover ./...
```

### Форматирование кода
```bash
# Форматирование
gofmt -w .

# Лinting
golangci-lint run ./internal/...
```

---

## 📚 Документация

### Основные файлы документации
- **`CODE_STRUCTURE.md`** — полное описание структуры проекта и всех компонентов
- **`PHASE_1_2_TYPES_REFERENCE.md`** — справочник всех типов данных Phase 1.2
- **`PHASE_1_3_DETAILED.md`** — подробное описание конфигурации Phase 1.3
- **`ARCHITECTURE.md`** (TODO) — архитектурные диаграммы и data flow
- **`DEVELOPMENT_PLAN.md`** (TODO) — детальный план развития

### Комментарии в коде
- ✅ Все файлы задокументированы на русском
- ✅ Функции содержат полное описание параметров и возвращаемых значений
- ✅ Критичные параметры отмечены (ОПАСНО!, РЕКОМЕНДУЕТСЯ!)

---

## 🐛 Известные проблемы

- Нет поддержки WebSocket (только REST)
- ClickHouse интеграция в разработке
- Backtesting только симуляция (реальная торговля в Phase 2+)

---

## 📝 Лицензия

Proprietary. Все права защищены.

---

## 👥 Автор

**Лев Титаев** (@titaev-lv)
- GitHub: https://github.com/titaev-lv

---

## 📞 Контакты

- Issues: https://github.com/titaev-lv/daemon2/issues
- Discussions: https://github.com/titaev-lv/daemon2/discussions

---

## 🔗 Ссылки

- [Go Documentation](https://golang.org/doc/)
- [MySQL Go Driver](https://github.com/go-sql-driver/mysql)
- [PostgreSQL Go Driver](https://github.com/lib/pq)
- [ClickHouse Go Client](https://github.com/ClickHouse/clickhouse-go)

---

**Последнее обновление:** 11 декабря 2024 г. | **Version:** 2.0.1
